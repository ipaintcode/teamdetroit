// going to be creating the carousel plugin with infinite carousel, pagenate count, video capabilities and ect...
(function($) {

	$.fn.fluidCarousel = function(options) {

		var settings = $.extend({
			
			speed: 500,
			buffer: 2,
			timer: false
			
		}, options);

		return this.each(function() {

		});

	};
	
})(jQuery);
